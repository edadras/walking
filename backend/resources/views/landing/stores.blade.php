@if (!empty($stores['bazaar']))<a class="btn primary" href="{{ $stores['bazaar'] }}" rel="noopener">دریافت از کافه‌بازار</a>@endif
@if (!empty($stores['myket']))<a class="btn secondary" href="{{ $stores['myket'] }}" rel="noopener">دریافت از مایکت</a>@endif
@if (!empty($stores['play']))<a class="btn secondary" href="{{ $stores['play'] }}" rel="noopener">دریافت از Google Play</a>@endif
@if (!empty($stores['apk']))<a class="btn secondary" href="{{ $stores['apk'] }}" rel="noopener">دانلود مستقیم (APK)</a>@endif
